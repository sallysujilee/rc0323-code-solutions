/* exported last */
function last(array) {
  return array[array.length - 1];
}

/*
Define a function, named `last`, that accepts one parameter, `array`.
  Return the last element of the array from the `array` array.
*/
