/* exported getValue */
function getValue(obj, key) {
  return obj[key];
}

/*
Define a function, named `getValue`, with two parameters, named `obj` and `key`
  Return the `obj` object with the `key` property.
*/
