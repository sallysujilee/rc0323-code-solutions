/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

/*
Define a function, named `setValue`, with three parameters `object`, `key`, and `value`.
  Assign the `object` variable with the `key` property to the `value` variable.
*/
